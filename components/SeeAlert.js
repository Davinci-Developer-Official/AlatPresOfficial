import {ScrollView,TouchableOpacity,Text,Image,StyleSheet} from 'react-native'
import{alertsList}from'./alerts.json'

export default function SeeAlert({single}) {
      const alerts = alertsList;
  return (
    <ScrollView style={{
      backgroundColor:"green",
    }} >
       <Text>Trtefadsh</Text>
      </ScrollView>
        
  )
}

